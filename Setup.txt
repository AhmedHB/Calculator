./gradlew build

docker build -t calculator .

docker run -p 8080:8080 --name calculator calculator

docker run -p 8080:8080 --name calculator calculator

http://localhost:8080/calculate-sum?a=1&b=2

see branches
git branch -a

behöver skapa credential i jenkins för att kommandot ovan ska fungera
git push origin Chapter5MorePipelineDocker


./gradlew build
docker build -t ahmedhb81/calculator .
docker run -d --rm -p 8765:8080 --name calculator ahmedhb81/calculator

curl "http://localhost:8080/calculate-sum?a=1&b=2" -s



Köra Jenkins

docker run -d -p 9000:8080 \
        --privileged \
        -v /var/run/docker.sock:/var/run/docker.sock \
        -v $HOME/jenkins_home:/var/jenkins_home \
        --name jenkins custom-jenkins-docker

docker run -d -p 9000:8080 \
        --network="bridge" \
        --privileged \
        -v /var/run/docker.sock:/var/run/docker.sock \
        -v $HOME/jenkins_home:/var/jenkins_home \
        --name jenkins custom-jenkins-docker



